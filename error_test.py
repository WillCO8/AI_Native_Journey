# This script is designed to cause a syntax error.
# The goal is to see the error message and learn to interpret it.

def greeting(name):
    print("Hello, " + name) # This line is correct

# This line has a missing closing parenthesis, causing a SyntaxError
# AI-suggested fix: Add the missing closing parenthesis.
greeting("World")
